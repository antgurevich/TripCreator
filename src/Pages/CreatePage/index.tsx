import "./styles.scss";

const baseClass = 'createpage'

export const CreatePage = () => {
    return (
      <div className={`${baseClass}`}>
        Create Page
      </div>
    );
}

export default CreatePage