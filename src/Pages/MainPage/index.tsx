import "./styles.scss"

const baseClass = 'mainpage';

export const MainPage = () =>{
    return( 
        <div className={`${baseClass}`}>
            Main Page
        </div>
    );
}

export default MainPage;